/*
    Food behavior class
*/

"use strict";

class Food extends PIXI.Sprite {
	// Initialize a new maggot
	constructor(texture) {
		super(texture);
		// this.x = x;
		// this.y = y;
        //
		// this.anchor.x = 0.75;
		// this.anchor.y = 0.5;
        //
		// this.velocity = {
		// 	x: 0,
		// 	y: 0,
		// };
        //
		// this.maxVelocity = 2.7;
		// this.maxForce = 0.27;
        //
		// // different maggots, different sizes
		// this
		// 	.scale
		// 	.set(0.8 + Math.random() * 0.3);
        //
		// this.tint = Math.random() * 0xFFFFFF;
	}

}
